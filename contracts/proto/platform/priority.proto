syntax = "proto3";

package telemetry.fabric.platform.v1;

// Priority controls how downstream services should handle an event under load.
enum Priority {
  PRIORITY_UNSPECIFIED = 0;
  PRIORITY_CRITICAL = 1;
  PRIORITY_IMPORTANT = 2;
  PRIORITY_AGGREGATE = 3;
  PRIORITY_DROP = 4;
}

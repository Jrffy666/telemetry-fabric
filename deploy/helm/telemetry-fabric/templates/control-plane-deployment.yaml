{{- if .Values.controlPlane.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "telemetry-fabric.controlPlaneName" . }}
  labels:
    {{- include "telemetry-fabric.controlPlaneLabels" . | nindent 4 }}
spec:
  replicas: {{ .Values.controlPlane.replicas }}
  {{- with .Values.controlPlane.strategy }}
  strategy:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "telemetry-fabric.controlPlaneName" . }}
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        {{- include "telemetry-fabric.controlPlaneLabels" . | nindent 8 }}
      {{- with .Values.controlPlane.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
    spec:
      serviceAccountName: {{ include "telemetry-fabric.controlPlaneServiceAccountName" . }}
      automountServiceAccountToken: {{ .Values.controlPlane.serviceAccount.automountServiceAccountToken }}
      terminationGracePeriodSeconds: {{ .Values.controlPlane.terminationGracePeriodSeconds }}
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- if .Values.controlPlane.priorityClassName }}
      priorityClassName: {{ .Values.controlPlane.priorityClassName | quote }}
      {{- end }}
      securityContext:
        {{- toYaml .Values.controlPlane.podSecurityContext | nindent 8 }}
      {{- with .Values.controlPlane.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.controlPlane.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.controlPlane.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- if .Values.controlPlane.databaseUrlSecret.name }}
      initContainers:
        - name: migrate
          image: "{{ .Values.controlPlane.image.repository }}:{{ .Values.controlPlane.image.tag }}"
          imagePullPolicy: {{ .Values.controlPlane.image.pullPolicy }}
          securityContext:
            {{- toYaml .Values.controlPlane.securityContext | nindent 12 }}
          command:
            - /opt/telemetry-fabric-control/bin/telemetry_fabric_control
          args:
            - eval
            - TelemetryFabricControl.Release.migrate()
          env:
            - name: TELEMETRY_FABRIC_CONTROL_DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.controlPlane.databaseUrlSecret.name | quote }}
                  key: {{ .Values.controlPlane.databaseUrlSecret.key | quote }}
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      {{- end }}
      containers:
        - name: control-plane
          image: "{{ .Values.controlPlane.image.repository }}:{{ .Values.controlPlane.image.tag }}"
          imagePullPolicy: {{ .Values.controlPlane.image.pullPolicy }}
          env:
            - name: TELEMETRY_FABRIC_CONTROL_HTTP_LISTEN
              value: {{ .Values.controlPlane.httpListen | quote }}
            - name: TELEMETRY_FABRIC_CONTROL_DATA_DIR
              value: {{ .Values.controlPlane.dataDir | quote }}
            - name: TELEMETRY_FABRIC_CONTROL_STORAGE
              value: {{ .Values.controlPlane.storage | quote }}
            - name: TELEMETRY_FABRIC_CONTROL_POSTGRES_SYNC
              value: {{ .Values.controlPlane.postgresSync | quote }}
            - name: TELEMETRY_FABRIC_CONTROL_POSTGRES_SYNC_INTERVAL_MS
              value: {{ .Values.controlPlane.postgresSyncIntervalMs | quote }}
            {{- with .Values.controlPlane.extraEnv }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
            {{- if .Values.controlPlane.databaseUrlSecret.name }}
            - name: TELEMETRY_FABRIC_CONTROL_DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.controlPlane.databaseUrlSecret.name | quote }}
                  key: {{ .Values.controlPlane.databaseUrlSecret.key | quote }}
            {{- end }}
            {{- if .Values.controlPlane.auth.agentTokenSecret.name }}
            - name: TELEMETRY_FABRIC_CONTROL_AGENT_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.controlPlane.auth.agentTokenSecret.name | quote }}
                  key: {{ .Values.controlPlane.auth.agentTokenSecret.key | quote }}
            {{- end }}
            {{- if .Values.controlPlane.auth.operatorTokenSecret.name }}
            - name: TELEMETRY_FABRIC_CONTROL_OPERATOR_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.controlPlane.auth.operatorTokenSecret.name | quote }}
                  key: {{ .Values.controlPlane.auth.operatorTokenSecret.key | quote }}
            {{- end }}
            {{- if .Values.controlPlane.tls.enabled }}
            - name: TELEMETRY_FABRIC_CONTROL_TLS_ENABLED
              value: "true"
            - name: TELEMETRY_FABRIC_CONTROL_TLS_CERT_FILE
              value: "{{ .Values.controlPlane.tls.mountPath }}/{{ .Values.controlPlane.tls.certKey }}"
            - name: TELEMETRY_FABRIC_CONTROL_TLS_KEY_FILE
              value: "{{ .Values.controlPlane.tls.mountPath }}/{{ .Values.controlPlane.tls.keyKey }}"
            - name: TELEMETRY_FABRIC_CONTROL_TLS_REQUIRE_CLIENT_AUTH
              value: {{ .Values.controlPlane.tls.requireClientAuth | quote }}
            {{- if .Values.controlPlane.tls.requireClientAuth }}
            - name: TELEMETRY_FABRIC_CONTROL_TLS_CA_FILE
              value: "{{ .Values.controlPlane.tls.mountPath }}/{{ .Values.controlPlane.tls.caKey }}"
            {{- end }}
            {{- end }}
          {{- with .Values.controlPlane.extraEnvFrom }}
          envFrom:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          ports:
            - name: http
              containerPort: {{ .Values.controlPlane.service.port }}
          {{- if gt (int .Values.controlPlane.lifecycle.preStopSleepSeconds) 0 }}
          lifecycle:
            preStop:
              exec:
                command:
                  - /bin/sh
                  - -c
                  - "sleep {{ .Values.controlPlane.lifecycle.preStopSleepSeconds }}"
          {{- end }}
          livenessProbe:
            {{ if .Values.controlPlane.tls.requireClientAuth }}
            tcpSocket:
              port: http
            {{ else }}
            httpGet:
              path: /healthz
              port: http
              scheme: {{ ternary "HTTPS" "HTTP" .Values.controlPlane.tls.enabled }}
            {{ end }}
            initialDelaySeconds: {{ .Values.controlPlane.probes.liveness.initialDelaySeconds }}
            periodSeconds: {{ .Values.controlPlane.probes.liveness.periodSeconds }}
            timeoutSeconds: {{ .Values.controlPlane.probes.liveness.timeoutSeconds }}
            failureThreshold: {{ .Values.controlPlane.probes.liveness.failureThreshold }}
          readinessProbe:
            {{ if .Values.controlPlane.tls.requireClientAuth }}
            tcpSocket:
              port: http
            {{ else }}
            httpGet:
              path: /readyz
              port: http
              scheme: {{ ternary "HTTPS" "HTTP" .Values.controlPlane.tls.enabled }}
            {{ end }}
            initialDelaySeconds: {{ .Values.controlPlane.probes.readiness.initialDelaySeconds }}
            periodSeconds: {{ .Values.controlPlane.probes.readiness.periodSeconds }}
            timeoutSeconds: {{ .Values.controlPlane.probes.readiness.timeoutSeconds }}
            failureThreshold: {{ .Values.controlPlane.probes.readiness.failureThreshold }}
          {{- if .Values.controlPlane.probes.startup.enabled }}
          startupProbe:
            {{ if .Values.controlPlane.tls.requireClientAuth }}
            tcpSocket:
              port: http
            {{ else }}
            httpGet:
              path: /healthz
              port: http
              scheme: {{ ternary "HTTPS" "HTTP" .Values.controlPlane.tls.enabled }}
            {{ end }}
            initialDelaySeconds: {{ .Values.controlPlane.probes.startup.initialDelaySeconds }}
            periodSeconds: {{ .Values.controlPlane.probes.startup.periodSeconds }}
            timeoutSeconds: {{ .Values.controlPlane.probes.startup.timeoutSeconds }}
            failureThreshold: {{ .Values.controlPlane.probes.startup.failureThreshold }}
          {{- end }}
          resources:
            {{- toYaml .Values.controlPlane.resources | nindent 12 }}
          securityContext:
            {{- toYaml .Values.controlPlane.securityContext | nindent 12 }}
          volumeMounts:
            - name: data
              mountPath: {{ .Values.controlPlane.dataDir | quote }}
            - name: tmp
              mountPath: /tmp
            {{- if .Values.controlPlane.tls.enabled }}
            - name: control-plane-tls
              mountPath: {{ .Values.controlPlane.tls.mountPath | quote }}
              readOnly: true
            {{- end }}
      volumes:
        - name: data
          {{- if and .Values.controlPlane.persistence.enabled (ne .Values.controlPlane.storage "postgres") }}
          persistentVolumeClaim:
            claimName: {{ include "telemetry-fabric.controlPlaneName" . }}-data
          {{- else }}
          emptyDir: {}
          {{- end }}
        - name: tmp
          emptyDir: {}
        {{- if .Values.controlPlane.tls.enabled }}
        - name: control-plane-tls
          secret:
            secretName: {{ .Values.controlPlane.tls.secretName | quote }}
        {{- end }}
{{- end }}
